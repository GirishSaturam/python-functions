def divideString(string,n):

  str_size = len(strng)
  
  if str_size % n != 0:
  
    print("Invalid input String sizeis not divisible by n")
    
    return
    
  partsize = str_size/n
  
  k=0
  
  for i in sring:
  
     if  %part_size == 0:
        
        print("/n"),
        
        print i,
        
        k+=1
        
   
   
